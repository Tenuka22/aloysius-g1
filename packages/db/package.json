{
  "name": "@aloysius-admissions/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./schools-catalog": {
      "default": "./src/schools-catalog.json"
    },
    "./*": {
      "default": "./src/*.ts"
    },
    "./scripts/*": {
      "default": "./scripts/*.ts"
    }
  },
  "scripts": {
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate:deploy": "bun --env-file ../../apps/web/.env scripts/backup-cli.ts && drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "bun --env-file ../../apps/web/.env scripts/backup-cli.ts && drizzle-kit migrate",
    "db:backup": "bun --env-file ../../apps/web/.env scripts/backup-cli.ts",
    "db:restore": "bun --env-file ../../apps/web/.env scripts/restore.ts",
    "db:auth": "bun scripts/generate-auth-schema.ts",
    "db:seed": "bun --env-file ../../apps/web/.env src/seed.ts"
  },
  "dependencies": {
    "@aloysius-admissions/env": "workspace:*",
    "better-sqlite3": "^13.0.3",
    "dotenv": "catalog:",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@aloysius-admissions/config": "workspace:*",
    "@types/better-sqlite3": "^9.6.0",
    "typescript": "catalog:"
  }
}
